# Versions are pinned exactly so parquet/CSV outputs are byte-identical across
# runs on the same Python version. Bump deliberately — and on published security
# advisories, which take precedence over pin stability. When a pin moves, the
# reproducibility guarantee holds within a pin set, not across pin sets.
aiohttp==3.14.3
httpx==0.27.0
pandas==2.2.2
numpy==1.26.4
pyarrow==23.0.1
pyyaml==6.0.1
python-dateutil==2.9.0.post0
tenacity==8.3.0
matplotlib==3.8.4
statsmodels==0.14.2
scipy==1.13.0
tqdm==4.66.4
pydantic==2.7.1
orjson==3.11.6
web3==7.15.0
eth-abi==5.1.0
